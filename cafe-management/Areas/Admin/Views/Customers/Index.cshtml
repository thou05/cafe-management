@* @using cafe_management.Models
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<TbCustomer>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Quản lí khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container" style="margin-bottom:20px;">
    <div class="row">
        <div class="col-lg-8 col-md-12 col-sm-12">
            <div class="page-header">
                <h3 class="page-title">Quản lí khách hàng</h3>
            </div>
        </div>
        <div class="col-lg-4 col-md-12 col-sm-12">
            <div class="search-field">
                @using (Html.BeginForm("Search", "Clients", FormMethod.Get))
                {
                    <div class="input-group">
                        <input type="text" class="form-control" name="search" style="border:none" placeholder="Tìm kiếm" autocomplete="off">
                        <button class="btn" type="submit"><i class="fa fa-search" aria-hidden="true"></i></button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <p>
            <a asp-action="Create" class="btn btn-secondary">Thêm mới</a>
        </p>
        <p id="status-message">@TempData["Message"]</p>
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Mã khách hàng</th>
                        <th>Tên khách hàng</th>
                        <th>Số điện thoại khách hàng</th>
                        <th>Địa chỉ</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PhoneNumber)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Address)
                            </td>
                            <td>
                                <a class="btn btn-success btn-sm" asp-action="Edit" asp-route-id="@item.Id" asp-route-name="@item.Name">Sửa</a>
                                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">Xoá</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    @Html.PagedListPager(Model, page=>Url.Action("index", new {page = page}), new PagedListRenderOptions()
    {
        MaximumPageNumbersToDisplay = 5,
        LiElementClasses = new List<String> {"page-item"},
        PageClasses = new List<String> {"page-link"}
    })
</div> *@

@* 
@using cafe_management.Models
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<TbCustomer>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Quản lý khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row">
    <div class="col-12">

        <div class="card shadow-sm">

            <div class="card-header card-header-flex">
                <div class="page-header mb-0">
                    @if (!String.IsNullOrEmpty(ViewBag.SearchTerm))
                    {
                        <h3 class="page-title mb-0">Kết quả cho: <span class="text-primary">@ViewBag.SearchTerm</span></h3>
                    }
                    else
                    {
                        <h3 class="page-title mb-0">Quản lý khách hàng</h3>
                    }
                </div>

                <div class="search-field">
                    @using (Html.BeginForm("Index", "Clients", FormMethod.Get))
                    {
                        <div class="input-group">
                            <input type="text" class="form-control" name="search" placeholder="Tìm kiếm tên, sđt..." autocomplete="off" value="@ViewBag.SearchTerm">
                            <button class="btn btn-primary" type="submit">
                                <i class="fa fa-search" aria-hidden="true"></i>
                            </button>
                        </div>
                    }
                </div>
            </div>

            <div class="card-body">
                <p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fa fa-plus"></i> Thêm khách hàng
                    </a>
                </p>

                @if (TempData["Message"] != null)
                {
                    var messageType = TempData["MessageType"] ?? "success";
                    <div class="alert alert-@messageType alert-dismissible fade show" role="alert">
                        @TempData["Message"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <div class="table-responsive">
                    <table class="table table-hover table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Mã KH</th>
                                <th>Tên khách hàng</th>
                                <th>Số điện thoại</th>
                                <th>Địa chỉ</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Id)
                                    </td>
                                    <td>
                                        <span class="fw-bold">@Html.DisplayFor(modelItem => item.Name)</span>
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.PhoneNumber))
                                        {
                                            <a href="tel:@item.PhoneNumber" class="text-decoration-none">@item.PhoneNumber</a>
                                        }
                                        else
                                        {
                                            <span class="text-muted">---</span>
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Address)
                                    </td>
                                    <td>
                                        <a class="btn btn-success btn-sm" asp-action="Edit" asp-route-id="@item.Id" asp-route-name="@item.Name" title="Sửa">
                                            <i class="fa fa-pencil"></i>
                                        </a>
                                        <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id" title="Xóa"
                                           onclick="return confirm('Bạn có chắc chắn muốn xóa khách hàng @item.Name không?');">
                                            <i class="fa fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            @if (Model.PageCount > 1)
            {
                <div class="card-footer">
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page, search = ViewBag.SearchTerm }), new PagedListRenderOptions()
                    {
                        MaximumPageNumbersToDisplay = 5,
                                LiElementClasses = new List<String> { "page-item" },
                                PageClasses = new List<String> { "page-link" },
                                })
            </div>
                        }
        </div>
    </div>
</div> *@
@using cafe_management.Models
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<TbCustomer>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Quản lý khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row">
    <div class="col-12">
        <div class="card shadow-sm">
            <div class="card-header card-header-flex">
                <div class="page-header mb-0">
                    @if (!String.IsNullOrEmpty(ViewBag.SearchTerm))
                    {
                        <h3 class="page-title mb-0">Kết quả cho: <span class="text-primary">@ViewBag.SearchTerm</span></h3>
                    }
                    else
                    {
                        <h3 class="page-title mb-0">Quản lý khách hàng</h3>
                    }
                </div>

                <div class="search-field">
                    @using (Html.BeginForm("Index", "Customers", FormMethod.Get))
                    {
                        <div class="input-group">
                            <input type="text" class="form-control" name="search"
                                   placeholder="Nhập tên hoặc số điện thoại..."
                                   autocomplete="off" value="@ViewBag.SearchTerm">
                            <button class="btn btn-primary" type="submit">
                                <i class="fa fa-search" aria-hidden="true"></i>
                            </button>
                        </div>
                    }
                </div>
            </div>

            <div class="card-body">
                <p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fa fa-plus"></i> Thêm khách hàng
                    </a>
                </p>

                @if (TempData["Message"] != null)
                {
                    var messageType = TempData["MessageType"] ?? "success";
                    <div class="alert alert-@messageType alert-dismissible fade show" role="alert">
                        @TempData["Message"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <div class="table-responsive">
                    <table class="table table-hover table-striped align-middle">
                        <thead>
                            <tr>
                                <th>Mã KH</th>
                                <th>Tên khách hàng</th>
                                <th>Số điện thoại</th>
                                <th>Địa chỉ</th>
                                <th class="text-center">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                                        <td><span class="fw-bold">@Html.DisplayFor(modelItem => item.Name)</span></td>
                                        <td>
                                            @if (!string.IsNullOrEmpty(item.PhoneNumber))
                                            {
                                                <a href="tel:@item.PhoneNumber" class="text-decoration-none">@item.PhoneNumber</a>
                                            }
                                            else
                                            {
                                                <span class="text-muted">---</span>
                                            }
                                        </td>
                                        <td>@Html.DisplayFor(modelItem => item.Address)</td>
                                        <td class="text-center">
                                            <a class="btn btn-success btn-sm" asp-action="Edit" asp-route-id="@item.Id" title="Sửa"><i class="fa fa-pencil"></i></a>
                                            <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id" title="Xóa" onclick="return confirm('Bạn có chắc chắn muốn xóa?');"><i class="fa fa-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr><td colspan="5" class="text-center py-4 text-muted">Không tìm thấy dữ liệu.</td></tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            @if (Model != null && Model.PageCount > 1)
            {
                <div class="card-footer">
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page, search = ViewBag.SearchTerm }), new PagedListRenderOptions()
                    {
                        MaximumPageNumbersToDisplay = 5,
                                LiElementClasses = new List<String> { "page-item" },
                                PageClasses = new List<String> { "page-link" },
                                })
            </div>
                        }
        </div>
    </div>
</div>